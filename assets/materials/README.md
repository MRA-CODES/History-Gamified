# materials

Put material files here: .tres resources created in Godot that define
how a surface looks (color, metalness, roughness, and which textures to
use).

Expected files:
- .tres material resources (for example StandardMaterial3D resources)

Materials can reference textures in res://assets/textures/.
