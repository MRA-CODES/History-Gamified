# props

Put smaller placeable 3D models (GLB) here: benches, lampposts, signs,
fences, trees, and other objects that fill a scene.

Expected files:
- .glb model files for props
- .gltf files from the same model pack

Props are environment objects only. Character models go in
res://assets/characters/.
